# Smoke-Test (15.08.): Fix A+B fuer Gemma-4 - Thinking-Steuerung kategorie-basiert.
# 1 Modell, DS1000 (Coding: enable_thinking=False) + MATH-500 (Math: enable_thinking=True).
# Verifiziert: kein <|think|> im Systemprompt (Bug B) + keine Thought-Channel im DS1000-Code.
# Lauf-Keys = lms ls --json modelKey (unique_key), NICHT Registry-Keys!

models:
  - "gemma-4-19b-a4b-it-reap-i1@q4_k_m"

benchmarks: ["DS1000", "MATH-500"]

sample_size: 5
seed: 2026
agentic_mode: "random"
thinking: false
no_structured_output: false
unload_between: false
keep_response: false
